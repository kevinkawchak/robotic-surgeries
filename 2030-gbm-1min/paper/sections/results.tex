% ======================================================================
% SECTION 3: RESULTS
% Six thematic subsections: instruction creation results, code
% generation results, code execution results (sensors, xyz, iterations,
% metrics, comparison), iteration results, competition results, and
% the exceptional-processing-feat highlight (54 columns by 1001 rows).
% Bracketed prompts for future Claude Code Opus 4.7 1M Max processing.
% Do not process the bracketed prompts here.
% ======================================================================

\subsection{Instruction Creation Results}
\label{sec:results-instructions}

[RESULTS SUBSECTION 1 PROMPT - PROCESS DURING THE PAPER POPULATION PASS]

[Document the outcome of the instruction-creation phase: 12
hand-authored Markdown files in
kevinkawchak/physical-ai-oncology-trials/competitions/instructions/one_minute_variant/
totaling roughly 130 KB. Render a LaTeX table using
{>{\raggedright\arraybackslash}p{Xcm}} for each column with the
following columns: file name, byte size, primary topic, downstream
commit it drives. Include all 12 files: README.md (14,255 bytes;
project narrative; commits 1 through 7), commit_01_overview_1min.md
(9,990 bytes; project overview; commit 1), commit_02_sensors_1min.md
(13,415 bytes; sensor specification; commit 2),
commit_03_xyz_4arm.md (18,750 bytes; xyz coordinate mapping; commit 3),
commit_04_iterations_1min.md (19,074 bytes; iteration sweep design;
commit 4), commit_05_competition_1min.md (20,808 bytes; tournament
and composite score formula; commit 5), file_size_pyramid_1min.md
(9,603 bytes; Git commit caps and Zenodo deposition; all commits),
glioblastoma_context_1min.md (8,285 bytes; IDH-wildtype GBM context;
abstract through introduction), multi_arm_coordination.md
(10,075 bytes; 1 kHz heartbeat broadcast bus protocol; commit 3),
robot_specification_neurospeed.md (12,504 bytes; 2030 Medtronic
NeuroSpeed 1.0 7-DOF DH parameters; commits 1 and 3),
sensor_specification_10khz.md (11,349 bytes; 50 channels per arm at
1 kHz commands plus 10 kHz force; commit 2),
zenodo_archive_protocol.md (8,994 bytes; Zenodo L0 deposition
protocol; release commit). Cite one-minute-variant-instructions for
the upstream pointer. State that no commits were made to the
upstream physical-ai-oncology-trials repository under this
generation. Highlight the readability achievement: a senior author
can read and verify all 12 instructions in approximately 1 hour.]

\subsection{Code Generation Results}
\label{sec:results-code-generation}

[RESULTS SUBSECTION 2 PROMPT - PROCESS DURING THE PAPER POPULATION PASS]

[Document the outcome of the code-generation phase: the
kevinkawchak/robotic-surgeries/2030-gbm-1min/ tree generated by Claude
Code Opus 4.7 1M Max in a single sequence of seven commits read
against the upstream instructions. Render the repository tree at the
file level using a verbatim block (scriptsize for fit). The
sub-directories to enumerate are: docs/ (architecture.md,
architecture_overview_4arm.txt, coordinate_mapping.md,
iteration_design.md, comparison_methodology.md,
multi_arm_coordination.md, sensor_spec.md, file_size_pyramid_1min.md),
config/ (project.yaml, kinematics_4arm.yaml, iterations.yaml),
schemas/ (sensor_record_4arm.{schema.json, proto, avsc},
xyz_command_4arm.{schema.json, proto}, metrics.schema.json), src/
(sensors/ingest_4arm.py, mapping/sensor_to_xyz_4arm.py,
control/robot_loop_4arm.cpp, coordination/arm_heartbeat.cpp,
simulation/{iterate_1min.py, runner_1min.rs, Cargo.toml},
metrics/compute_1min.py, llm/compare_agent_1min.py,
zenodo/patch_pointers.py), data/ (sensor_sample_4arm.{jsonl, csv},
sensor_l0_raw_4arm.zenodo_pointer.json,
xyz_trace_sample_arm{1..4}.csv,
xyz_trace_4arm.zenodo_pointer.json, human_surgeon_baseline.csv,
robot_outcomes_1min.parquet, iterations/), prompts/
(comparison_prompt_1min.md), results/ (comparison.{json, md, pdf}),
viz/ (xyz_path_4arm.txt, metrics_dashboard.html, metrics_summary.png,
per_arm_contribution.png), notebooks/
(iteration_analysis_1min.ipynb), logs/ (iteration_run.txt), and
releases/v3.9.1/ (manifest.json, metrics.json,
iterations_index.jsonl, sample_seeds.txt, zenodo_doi.txt). Cite
repo-robotic-surgeries for the DOI 10.5281/zenodo.18445179 and the
GitHub URL. Note the committed footprint of approximately 8.2 MB
plus 1.5 MB of fixed overhead inside the 10 MB GitHub commit cap.
State that the C++20 control loop, C++20 1 kHz heartbeat layer, and
Rust 2021 high-throughput runner are documented but not necessarily
compiled in this generation pass.]

\subsection{Code Execution Results: 60-Second Surgery Outputs}
\label{sec:results-execution}

[RESULTS SUBSECTION 3 PROMPT - PROCESS DURING THE PAPER POPULATION PASS]

[Document the outcome of the code-execution phase: every artifact
under kevinkawchak/robotic-surgeries/2030-gbm-1min/outputs/ produced
by the end-to-end run under deterministic seed 20260510. Enumerate
each subdirectory and call out the exact file names. (i) sensors/:
sensor_sample_4arm.csv (54 columns by 1001 rows, 328,639 bytes;
THIS IS THE EXCEPTIONAL PROCESSING FEAT THAT NO HUMAN TEAM COULD
PRODUCE IN DUE TIME), sensor_sample_4arm.jsonl (891,992 bytes
companion with full per-arm record), summary.json (902 bytes per-arm
and aggregate force statistics; per_arm_violations=0
cumulative_violations=0 against the 5.0 N per-arm tip and 12 N
cumulative-four-arm-tip envelope), README.md. (ii) xyz_mapping/:
xyz_trace_sample_arm1.csv (6,519 bytes), xyz_trace_sample_arm2.csv
(6,632 bytes), xyz_trace_sample_arm3.csv (6,737 bytes),
xyz_trace_sample_arm4.csv (6,823 bytes), xyz_path_4arm.txt
(4,740 bytes ASCII per-second xyz path overlay), summary.json
(1,584 bytes), README.md. All 240 commands resolve to
command_state=EMIT. (iii) iterations/: 16 L1 50 ms Parquet plus 16
L2 1 s Parquet plus 16 L3 phase Parquet plus 16 events Parquet plus
16 L0 Zenodo pointer JSON, cross-iteration index.jsonl manifest,
aggregate.duckdb analytical store, README.md. (iv) metrics/:
robot_outcomes_1min.parquet (per-iteration metric rows for 16 robot
plus 30 human baseline under the frozen weighted formula), README.md.
(v) comparison/: comparison.json (default robot vs robot, 6 rounds),
comparison_report.md, comparison_report.pdf, README.md. (vi)
comparison_robot_vs_human/: comparison.json (mixed 2 robot plus 2
human, 6 rounds), comparison_report.md, comparison_report.pdf,
README.md; structural-time-dimension caveat preserved. (vii)
diagrams/: 6 curated ASCII diagrams (pipeline architecture, 4-arm
coordination heartbeat, 60-second phase timeline, file size pyramid,
composite score formula plus aggregates, on-prem LLM control loop
instantiating the thesis), README.md. (viii) viz/: ASCII bar /
histogram charts (composite per iteration, composite histogram
robot vs human, per-arm resection mean in mm cubed, wall-clock per
iteration), HTML dashboard, PNG charts mirrored from upstream viz/,
README.md. (ix) reports/: run_summary.md, process_log.md,
final_report.md, limitations.md, README.md. (x) logs/: per-script log
files plus ci_verification.log, README.md. Render the file inventory
as a LaTeX longtable using {>{\raggedright\arraybackslash}p{Xcm}} for
each column. Cite zenodo for the L0 deposition and repo-robotic-surgeries
for the DOI.]

\subsection{16-Iteration Deterministic Sweep Results}
\label{sec:results-iterations}

[RESULTS SUBSECTION 4 PROMPT - PROCESS DURING THE PAPER POPULATION PASS]

[Document the outcome of the 16-iteration deterministic sweep. Read
kevinkawchak/robotic-surgeries/2030-gbm-1min/outputs/iterations/index.jsonl
and the per-iteration L1 50 ms, L2 1 s, L3 phase, and events Parquet
aggregates. Render a per-iteration LaTeX table using
{>{\raggedright\arraybackslash}p{Xcm}} for each column with at least
the following columns: iteration index, seed, noise sigma (mm),
force feedback gain, IK tolerance, heartbeat jitter (microseconds),
wall-clock seconds, cumulative-arm-force max (N), per-arm tip force
max (N), heartbeat exceedances, composite score. State that bit
determinism holds for fixed seed plus parameter tuple, wall-clock
ranges 26 to 32 seconds on the M3 Ultra recipe, cumulative four-arm
force violations remain bounded by the 12 N envelope across the seed
sweep with the bulk of violations concentrated in Phase 2 where arm
1 cuts at 800 mm cubed per second peak, heartbeat jitter from 0 to
50 microseconds yields zero exceedances of the 3 ms watchdog
threshold, and per-arm contribution analysis confirms arm 1 dominates
resection volume (32,400 mm cubed mean) while arms 2 to 4 stay within
the 30 percent target balance band on coagulation seconds, suction
volume, and imaging frames respectively. Cite apache-arrow for the
Parquet format and duckdb for the cross-iteration analytical store.]

\subsection{LLM Tournament Competition Results}
\label{sec:results-competition}

[RESULTS SUBSECTION 5 PROMPT - PROCESS DURING THE PAPER POPULATION PASS]

[Document the outcome of the on-premises LLM tournament. Read
kevinkawchak/robotic-surgeries/2030-gbm-1min/outputs/comparison/comparison.json
(default robot vs robot, 6 rounds) and
kevinkawchak/robotic-surgeries/2030-gbm-1min/outputs/comparison_robot_vs_human/comparison.json
(mixed 2 robot plus 2 human, 6 rounds). Render two LaTeX tables
using {>{\raggedright\arraybackslash}p{Xcm}} for each column. The
first table is the default 4-entity tournament leaderboard with
columns: entity ID, mean composite score, mean quality, mean time,
mean cost, mean safety, mean patient experience, rounds won. The
second table is the mixed 4-entity tournament leaderboard with the
same columns plus a entity-type column (robot or human). State
explicitly the headline result: robot mean composite score 88.53 vs
human mean composite 70.35, robot wins all 4 robot-vs-human pairings
with confidence 0.955 to 1.000, structural-time-dimension caveat
preserved in every rationale. Cite claude-opus-47, claude-code, and
ollama for the judging stack and prompts/comparison_prompt_1min.md
for the versioned prompt.]

\subsection{Exceptional Processing Feat: 54 columns by 1001 rows}
\label{sec:results-feat}

[RESULTS SUBSECTION 6 PROMPT - PROCESS DURING THE PAPER POPULATION PASS]

[Open with the headline sentence: no human team could have produced
the 54 columns by 1001 rows sensor sample table at
kevinkawchak/robotic-surgeries/2030-gbm-1min/outputs/sensors/sensor_sample_4arm.csv
within the time budget of this paper. Render a small representative
slice as a LaTeX table using {>{\raggedright\arraybackslash}p{Xcm}}
for each column (8 to 12 sample columns from arm 1 and arm 4, 10 to
20 sample rows from Phase 1, Phase 2, and Phase 3). State the file
size (328,639 bytes for CSV and 891,992 bytes for the JSONL
companion). State that each of the 1001 time rows corresponds to a
mixed 1 kHz command tick plus a 10 kHz force snapshot decimated to
1 kHz for the sample, that 50 sensor channels per arm are flattened
into the columns, and that 4 metadata columns (timestamp_us, arm_id,
phase, command_state) bring the column count to 54. Enumerate other
significant feats that could not be done without state of the art AI
under the time budget: (a) the 240-row per-arm xyz command trace
across 4 files at
kevinkawchak/robotic-surgeries/2030-gbm-1min/outputs/xyz_mapping/
plus the ASCII path overlay; (b) the 16-iteration deterministic
sweep with bit-determinism for fixed seed plus parameter tuple at
outputs/iterations/; (c) the 6 curated ASCII diagrams at
outputs/diagrams/ and the 4 ASCII bar / histogram charts at
outputs/viz/; (d) the 4 narrative reports at outputs/reports/ and
the ci_verification.log at outputs/logs/; (e) the structured
comparison.json plus markdown plus PDF report at
outputs/comparison/ and outputs/comparison_robot_vs_human/ produced
under the on-premises LLM tournament; (f) the cross-file consistency
across docs, config, schemas, src, data, prompts, results, viz,
notebooks, logs, and releases that no human team could maintain
under the 1 PR 7 commit cap. Cite claude-opus-47, claude-code, and
repo-robotic-surgeries.]
