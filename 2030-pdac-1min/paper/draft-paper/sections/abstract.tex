% ======================================================================
% ABSTRACT (title-page block, single paragraph approximately 900 chars)
% Location: 2030-pdac-1min/paper/draft-paper/sections/abstract.tex
%
% [DOWNSTREAM CLAUDE CODE PROCESSING INSTRUCTION - DO NOT EXECUTE NOW]
% Read the instruction set, codegen tree, execution tree, and input
% papers in the order below and synthesize a single 900 to 1000
% character paragraph that opens with the on-premises repository based
% LLM thesis (commands to standard oncology surgical robots based on
% real-time sensor data, controlled via x, y, z coordinates), names
% the four-phase Claude Code workflow (instructions plus codegen plus
% execution plus draft paper), names the 60 second 8 arm Whipple
% target, names the Daraxonrasib precision oncology adjuvant, and
% closes on the practical real-life adoption gap (synthetic patient,
% TRIPOD+AI plus CREMLS reporting floor, FDA SaMD opportunism).
%
% Input directories and files to process for the abstract:
%   1. 2030-pdac-1min/paper/instructions/README.md
%      (Pull the thesis paragraph and the disease-impact paragraph
%      verbatim through paraphrase; keep the 8-phase 60-second timing
%      and the per-iteration 980 KB pyramid budget intact.)
%   2. 2030-pdac-1min/paper/instructions/pdac_context_1min.md
%      (Pull PAT-PDAC-0001 patient summary; keep the KRAS G12D
%      mutation status and Daraxonrasib eligibility front and center.)
%   3. 2030-pdac-1min/paper/instructions/robot_specification_pancrespeed.md
%      (Pull the PancreSpeed 1.0 8-arm 1,200 mm/s 100 kHz force 0.05
%      mm RMS at 1,200 mm/s 3 ms E-stop headline numbers.)
%   4. 2030-pdac-1min/paper/instructions/sensor_specification_100khz.md
%      (Pull the 640 channel total: 80 ch/arm x 8 arms.)
%   5. 2030-pdac-1min/paper/codegen/README.md
%      (Pull the v0.6.0 codegen contract that the execution tree was
%      run against.)
%   6. 2030-pdac-1min/paper/execution/README.md
%      (Pull the v0.7.0 mean composite 93.298, std 1.225, 95 percent
%      CI half width 0.462 headline numbers and the 4 entrant
%      tournament leaderboard 93.735 mean win.)
%   7. 2030-pdac-1min/paper/execution/sensors/sensor_sample_8arm.jsonl
%      (Highlight the 1001-record Phase 5 first 100 ms sample as the
%      single largest exceptional-processing feat.)
%   8. 2030-pdac-1min/paper/inputs/daraxonrasib-1/main.tex
%      (Pull the Daraxonrasib historical anchor: FDA Breakthrough
%      06/25, RASolute 302 + RASolve 301 program names, doi
%      10.5281/zenodo.18099351.)
%
% Output target: one paragraph, 900 to 1000 characters, no bullet
% lists, no display math. Single dashes only. Black text only.
% Include the 5x to 500x performance gap framing inherited from the
% v0.4.0 GBM paper abstract pattern at
% 2030-gbm-1min/paper/full-paper/final-paper/sections/abstract.tex.
% Do not introduce SS for the section sign; use \S where applicable.
% Do not exceed the 1000 character cap; the abstract block must fit
% on the title page below the author block and above the disclaimer.
% ======================================================================

[ABSTRACT PLACEHOLDER. The downstream Claude Code Opus 4.7 1M Max
session will replace this single paragraph with the 900 to 1000
character abstract synthesized from the eight inputs listed above in
the bracketed instruction block. The abstract must open with the
on-premises repository based LLM thesis, name the four-phase Claude
Code workflow (instructions plus codegen plus execution plus draft
paper), name the 60 second 8 arm Whipple target plus the
Daraxonrasib precision oncology adjuvant, cite the 1001 record Phase
5 first 100 ms sensor_sample_8arm.jsonl exceptional-processing feat,
and close on the practical adoption gap. The mean composite 93.298
across 32 iterations and the leaderboard 93.735 must be retained.]
